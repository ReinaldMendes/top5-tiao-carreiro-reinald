// src/types/Song.ts
export interface Song {
    id: number;
    title: string;
    artist: string;
    youtube_link: string;
    // Adicione outros campos se necessário, como 'suggested_by', 'is_approved', etc.
}